.DS_Store
*.backup*
*.swp
*.tmp

# Machine-specific files, generated from the .example templates by install.sh
ssh/config
# ~/.gitconfig itself lives in $HOME, never in this repo — see git/gitconfig.example

# Security / secrets safety guard
id_rsa*
id_ed25519*
*.key
*.keystore
*.pem
*.token
.env*
.claude.json
